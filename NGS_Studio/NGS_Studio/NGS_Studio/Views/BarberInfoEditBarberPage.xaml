<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NGS_Studio.Views.BarberInfoEditBarberPage"
             xmlns:vm="clr-namespace:NGS_Studio.ViewModels"
             Title="{Binding Title}" BackgroundColor="Black">

    <ContentPage.BindingContext>
        <vm:BarberInfoEditBarberViewModel />
    </ContentPage.BindingContext>

    <StackLayout Margin="20,35,20,20">

        <Entry x:Name="nameEntry"
               Placeholder="Enter name" 
               Text = "{Binding Barber.Name, Mode=TwoWay}"
               PlaceholderColor="DarkGray"
               TextColor="{OnPlatform iOS=Black, Android=White}"/>
        <Entry x:Name="emailEntry"
               Placeholder="Enter email" 
               Text = "{Binding Barber.Email, Mode=TwoWay}"
               PlaceholderColor="DarkGray"
               TextColor="{OnPlatform iOS=Black, Android=White}"/>
        <Entry x:Name="phoneNumberEntry"  
               Keyboard="Numeric" 
               Placeholder="Phone Number"
               Text = "{Binding Barber.PhoneNumber, Mode=TwoWay}"
               PlaceholderColor="DarkGray"
               TextColor="{OnPlatform iOS=Black, Android=White}">
            <Entry.Behaviors>
                <vm:MaskedBehavior Mask="( XXX ) XXX - XXXX" />
            </Entry.Behaviors>
        </Entry>
        <Button Text="Update"
                Command="{Binding EditBarberCommand}"
                BackgroundColor="DarkGoldenrod"
                />
        
    </StackLayout>
    
</ContentPage>